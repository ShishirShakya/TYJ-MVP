# Python version requirement
# Requires Python >=3.12

# FastAPI and server
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP client
requests>=2.31.0
httpx>=0.25.2

# Environment variables
python-dotenv>=1.2.1

# Security
python-multipart>=0.0.6

# Cryptography (for encryption/decryption)
cryptography>=41.0.0

# Audio processing (mandatory - for voiceprint, stylometry)
librosa>=0.10.0
mutagen>=1.47.0

# Proctoring (mandatory - for face detection, gaze analysis)
mediapipe>=0.10.0

# Numerical computing (required by librosa/mediapipe)
numpy>=1.26.0

# AI/LLM (for question generation, grading, transcription)
openai>=1.68.2

# Data processing
pandas>=2.0.0

# PDF processing
pdfplumber>=0.11.0

# Logging
structlog>=24.1.0

# UI framework (for Hugging Face Spaces deployment)
gradio>=5.22.0,<6.0.0  # Pin to major version 5.x to prevent breaking changes

# Phase 4.1: Load Testing
locust>=2.17.0

# Phase 4.2: Monitoring & Alerting
prometheus-client>=0.19.0